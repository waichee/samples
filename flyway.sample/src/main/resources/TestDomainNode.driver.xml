<?xml version="1.0" encoding="utf-8"?>
<driver project-version="3.0.0.1" class="${flyway.sample.db.driver}">
	<url value="${flyway.sample.db.url}"/>
	<connectionPool min="1" max="50"/>
	<login userName="${flyway.sample.db.username}" password="${flyway.sample.db.password}"/>
</driver>